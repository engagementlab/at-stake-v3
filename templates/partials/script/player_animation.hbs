{{!< default}}
{{!-- 
/**
 * @Stake v3
 * Developed by Engagement Lab, 2016
 * ==============
 * 
 * Script include for global logic
 * ==========
 */
--}}

function playerEntry() {

	var animTiming = [.5, .6, .7, .8];
	var entryButtons = $('.gameLanding a');
	
  _.each(entryButtons, function(el, index) {

		timeline.add(TweenMax.fromTo(el, animTiming[index], {scale:0}, {scale:1, ease:Elastic.easeOut}));

  });

  timeline.play();

}

function rulesAnim() {

	var screens = $('.row.screen');
  var screensAnim = new TimelineLite({ paused: true, onComplete: 
																										  	function() { TweenLite.from($('#btn-next'), 1, {scale:0, autoAlpha:0, ease:Elastic.easeOut}); } 
																		});
	
  _.each(screens, function(el, index) {

		screensAnim.fromTo(el, .5, {xPercent:-100}, { x:0, xPercent:0, ease:Expo.easeInOut, opacity: 1, display:'block', delay:.5 });

		if(index < screens.length-1)
			screensAnim.fromTo(el, .5, {xPercent:0}, { x:200, xPercent:200, ease:Expo.easeInOut, opacity:0, display:'none', delay:6 });

  });

  screensAnim.play();
	
}